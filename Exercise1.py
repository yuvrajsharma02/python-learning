#here we create a formula to calculate the area of retange by the user input data 

length = float(input("Enter the length: "))  # here we can typecast the data type because it can not take string as a number 
width = float(input("Enter the width: ")) # so we can type cast it as a float beacuse it can take both value int and float
area = length *  width 

print(f" the area is : {area}cm")
 
 